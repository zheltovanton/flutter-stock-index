name: flutter_stock_index
description: Stock App

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# Read more about versioning at semver.org.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  firebase_core:
    path: ../plugins/packages/firebase_core
  firebase_messaging:
    path: ../plugins/packages/firebase_messaging
  firebase_analytics:
      path: ../plugins/packages/firebase_analytics/
  date_utils: ^0.1.0
  flutter_webview_plugin:
    path: ../flutter-examples2/flutter_webview_plugin/

dev_dependencies:
  flutter_test:
    sdk: flutter
  async_loader: "^0.1.1"
  http: ^0.12.0
  google_maps_webservice: "^0.0.4"
  shared_preferences: "^0.4.2"

flutter:
  uses-material-design: true
